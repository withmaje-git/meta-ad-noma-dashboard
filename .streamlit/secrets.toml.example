# ────────────────────────────────────────────────────────────────
# 이 파일을 복사해서 secrets.toml 로 이름을 바꾼 뒤 값을 채우세요.
# Streamlit Cloud 에서는 앱 Settings > Secrets 에 아래 한 줄을 붙여넣습니다.
# secrets.toml 은 절대 GitHub 에 올리지 마세요(.gitignore 에 포함됨).
# ────────────────────────────────────────────────────────────────

# 관계자에게 공유할 열람용 비밀번호 (없으면 잠금 없이 공개)
app_password = "여기에_공유용_비밀번호"
